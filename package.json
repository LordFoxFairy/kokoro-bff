{
  "name": "@kokoro/bff",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.25.0",
  "engines": {
    "node": ">=22 <23"
  },
  "type": "module",
  "scripts": {
    "dev": "node --watch --experimental-strip-types src/main.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "db:apply-schema": "node scripts/apply-schema.mjs",
    "lint": "node scripts/lint-source.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "contract:lint": "redocly lint contract/openapi/v1/openapi.yaml",
    "contract:test": "node --test test/contract-governance.test.mjs",
    "contract:check": "pnpm contract:lint && pnpm contract:test",
    "contract:update-baseline": "node scripts/check-contract.mjs --write-baseline",
    "test:architecture": "pnpm build && node --test test/architecture.test.ts",
    "test": "pnpm build && node --test test/architecture.test.ts test/contract-governance.test.mjs test/schema-governance.test.mjs test/agui.test.ts test/bff.test.ts test/config.test.ts test/mori.test.ts test/music.test.ts test/scheduler.test.ts test/upstream.test.ts",
    "test:integration": "pnpm build && node --test --test-concurrency=1 test/business-store.integration.mjs test/agui-http.integration.mjs test/agui-projection.integration.mjs",
    "check": "pnpm lint && pnpm typecheck && pnpm contract:check && pnpm test && pnpm build"
  },
  "devDependencies": {
    "@redocly/cli": "2.46.1",
    "@types/node": "^22.15.0",
    "@types/pg": "^8.23.1",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@ag-ui/core": "0.0.59",
    "pg": "^8.23.0",
    "redis": "^5.12.1"
  }
}
