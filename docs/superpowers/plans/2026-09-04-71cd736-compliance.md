# kokoro-bff 71cd736 Compliance Fix Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close every P1/P2 defect found in commit `71cd736` without adding assistant source-to-Message reconciliation.

**Architecture:** Keep PostgreSQL as the only durable Chat, dispatch, cancellation, and AG-UI truth. Pass trusted tenant and subject through the private Chat boundary, allocate ordering facts while the conversation row is locked, use one immediately processed fenced lease per worker claim, and model remote cancellation as its own durable Agent control command. Public share reads remain a separate capability service.

**Tech Stack:** Node.js 22, strict TypeScript, PostgreSQL, Redis DB 8, Node test runner, OpenAPI 3.1.

## Global Constraints

- Write only `/Users/nako/WebstormProjects/github/thefoxfairy/Kokoro/kokoro-bff`.
- Never read, modify, stage, overwrite, or commit `docs/api/v1/agui-chat.md`.
- Do not add migrations, foreign keys, `REFERENCES`, fallbacks, or dual-read paths.
- Keep every database instant as `TIMESTAMPTZ(3)` and every SQL value parameterized.
- Do not implement assistant source-to-Message reconciliation.

---

### Task 1: Owner-scoped Chat admission and canonical HTTP semantics

**Files:**
- Modify: `src/application/ports/chat-repository.ts`, `src/application/chat-service.ts`, `src/http/routes/chat.ts`
- Create: `src/application/ports/public-share-repository.ts`, `src/application/public-share-service.ts`, `src/application/chat/message-create-input.ts`, `src/infrastructure/postgres/public-share-repository.ts`
- Modify: `src/infrastructure/postgres/chat-repository.ts`, `src/infrastructure/postgres/chat-repository-mappers.ts`, `src/application/services.ts`, `src/infrastructure/postgres/repositories.ts`
- Modify: `src/application/idempotency.ts`, `src/http/request.ts`, `src/bootstrap/server.ts`, `contract/openapi/v1/openapi.yaml`
- Test: `test/chat-service.test.ts`, `test/chat-input.test.ts`, `test/idempotency.test.ts`, `test/chat-facts.integration.mjs`

**Interfaces:**
- Private Chat repository methods consume `(tenantId, subjectId, ...)`.
- `PublicShareRepository` exposes only `findActiveShare` and `listSharedMessages` without a subject.
- `parseMessageCreateRequest(json, queryProjectRef)` returns a fully trimmed `MessageCreateInput` or a stable validation error.
- `mutationTicket` scopes receipts by trusted namespace and subject; Chat message admission bypasses the outer receipt and uses its transactional outbox identity.

- [ ] Write owner, strict-input, cursor, and semantic-idempotency regression tests.
- [ ] Run the focused tests and confirm they fail for the missing behavior.
- [ ] Implement the owner boundary, bigint-safe cursor, parser, and canonical fingerprint.
- [ ] Run focused unit/contract tests, typecheck, and `git diff --cached --check`.
- [ ] Commit `fix(chat): enforce owner-scoped durable admission`.

### Task 2: Dispatch FIFO, lease budget, and deterministic delivery classification

**Files:**
- Modify: `database/schema.sql`, `src/domain/chat/agent-dispatch.ts`
- Modify: `src/application/ports/agent-dispatch-outbox-repository.ts`, `src/application/ports/agent-dispatch-delivery.ts`
- Modify: `src/application/agent-dispatch-outbox-dispatcher.ts`
- Modify: `src/infrastructure/postgres/agent-dispatch-outbox-repository.ts`, `src/infrastructure/clients/agent/outbox-delivery.ts`
- Test: `test/agent-dispatch-outbox.test.ts`, `test/chat-facts.integration.mjs`, `test/schema-governance.test.mjs`

**Interfaces:**
- Each admitted launch persists `conversation_dispatch_seq`, equal to that turn's user `message_seq`, as a decimal string outside SQL.
- `claimAgentDispatchOutbox` receives `maxAttempts` and returns one immediate lease with a database-clock `leaseRemainingMs` budget.
- `AgentDispatchDeliveryPort.deliver(command, timeoutBudgetMs)` must keep owner HTTP timeout inside the remaining lease reserve.

- [ ] Write FIFO, one-at-a-time claim, exhausted-before-claim, timeout-budget, and classification tests.
- [ ] Confirm focused tests fail.
- [ ] Implement schema, claim, dispatcher, and delivery changes.
- [ ] Run focused tests, schema gate, typecheck, and staged-diff checks.
- [ ] Commit `fix(outbox): enforce fenced FIFO dispatch`.

### Task 3: Durable failure convergence and delete compensation

**Files:**
- Create: `src/domain/chat/agent-cancellation.ts`
- Create: `src/application/ports/agent-cancellation-outbox-repository.ts`, `src/application/ports/agent-cancellation-delivery.ts`, `src/application/agent-cancellation-outbox-dispatcher.ts`
- Create: `src/infrastructure/postgres/agent-cancellation-outbox-repository.ts`, `src/infrastructure/clients/agent/cancellation-delivery.ts`
- Modify: `database/schema.sql`, `src/infrastructure/postgres/chat-repository.ts`, `src/infrastructure/postgres/agent-dispatch-outbox-repository.ts`
- Modify: `src/application/agui/project-chat-event.ts`, `src/http/routes/agent.ts`, `src/bootstrap/runtime.ts`
- Test: `test/agent-dispatch-outbox.test.ts`, `test/chat-facts.integration.mjs`, `test/agui-http.integration.mjs`

**Interfaces:**
- Permanent launch failure writes one BFF-owned `RUN_ERROR`, sets `terminal_run_id` only when the failed run is still expected, stops and fences that consumer, and fails its provisional assistant in the same transaction.
- Conversation deletion writes stable `run.cancel` commands with `cancel_requested` state for every possibly admitted run; cancellation uses its own token/fence/retry lifecycle and retries `404` while run admission is uncertain.

- [ ] Write terminal-ledger and delete-during-delivery regression tests.
- [ ] Confirm the tests fail.
- [ ] Implement transactional failure projection and durable cancellation outbox.
- [ ] Run focused unit/integration tests and staged-diff checks.
- [ ] Commit `fix(chat): converge failed and deleted Agent runs`.

### Task 4: Agent control contract and executable OpenAPI gates

**Files:**
- Create: `contract/external/kokoro-agent/control-receipt.v1.json`, `src/infrastructure/clients/agent/control-receipt.ts`
- Modify: `src/http/routes/agent.ts`, `scripts/verify-openapi.ts`, `package.json`, `contract/openapi/v1/openapi.yaml`, `contract/README.md`
- Test: `test/agent-control-adapter.test.ts`, `test/contract/openapi-contract.test.mjs`, `test/contract-governance.test.mjs`

**Interfaces:**
- Parse Agent `ControlReceipt` as exactly `command_id`, `request_digest`, `status`, optional `error_code`, and `replayed`; synthesize public `run_id` from the trusted URL parameter.
- `contract:check` runs Redocly, governance, and semantic verifier tests including `Gone`, `413`, and `503` parity.

- [ ] Write adapter and semantic-gate regressions and confirm failure.
- [ ] Add the pinned owner snapshot, strict parser, route mapping, and gate wiring.
- [ ] Run `pnpm contract:check`, focused tests, typecheck, and staged-diff checks.
- [ ] Commit `fix(contract): pin Agent control receipt parity`.

### Task 5: Awaitable shutdown, evidence, and final verification

**Files:**
- Modify: `src/bootstrap/runtime.ts`, `src/bootstrap/server.ts`, `src/main.ts`, `test/doubles/server.ts`
- Modify: integration close helpers and focused lifecycle tests
- Modify: `README.md`, `INDEX.md`, `docs/CURRENT.md`, `docs/TECHNICAL_DESIGN.md`, `docs/DATA_MODEL.md`, `docs/RELIABILITY.md`, `docs/ACCEPTANCE.md`, `docs/API_CONTRACT.md`

**Interfaces:**
- `BffServer.shutdown(deadlineMs?)` stops accepting, waits for worker drain under a deadline, then closes PostgreSQL/Redis exactly once.
- SIGTERM/SIGINT await the same shutdown path and set process exit state only after it settles.

- [ ] Write an async shutdown ordering test and confirm failure.
- [ ] Implement the awaitable lifecycle and update every integration helper to await it.
- [ ] Correct documentation claims and test evidence without changing the forbidden collaborator file.
- [ ] Run all local and real-fixture gates, root BFF audit, and clean-worktree checks.
- [ ] Commit `fix(runtime): drain workers before closing stores`.
